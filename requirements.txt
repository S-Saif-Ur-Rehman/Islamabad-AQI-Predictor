# Core app and data tools
# (No Flask/gunicorn on this branch — single-tier streamlit-only architecture,
# no separately-hosted API. See the `master` branch for the render+streamlit setup.)
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
pandas>=2.1.0,<2.4.0
numpy>=1.26.3,<2.0.0
pyarrow>=14.0.0

# Modeling and inference
scikit-learn>=1.4.0
joblib>=1.3.2
shap>=0.44.0,<0.49.0
scipy>=1.12.0,<1.14.0
hopsworks==5.0.6
protobuf==4.25.9

# TensorFlow for local training
tensorflow-cpu==2.16.2



